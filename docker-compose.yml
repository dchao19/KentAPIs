version: "2"
services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
    links:
      - mongo
    depends_on:
      - "mongo"

