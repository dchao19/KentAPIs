-function prettyDate(date){
-var d = date.getDate();
-var monthNames = [ "Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec" ];
-var m = monthNames[date.getMonth()];
-var y = date.getFullYear();
-return d+' '+m+' '+y;
-}

-function prettyTime(date){
-var h = date.getHours();
-var m = date.getMinutes();
-var str_m = "" + m;
-str_m = (str_m.length == 1) ? '0' + str_m : str_m;
-return h+':'+str_m;
-}

-function prettyPeriod(period){
-var d1 = period.start_time;
-var d2 = period.end_time;

-return prettyTime(d1) + " - " + prettyTime(d2);
-}

-function isoDate(date){
-return date.toISOString();
-}

-function dateTimeStr(date){
-var s = isoDate(date);
-return s.substring(0, s.length-1);
-}

-function dateStr(date){
-var s = isoDate(date);
-return s.substring(0, 10);
-}
-var dateOptions = {year: "numeric"}

html
    head
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='viewport', content='width=device-width, initial-scale=1.0, minimum-scale=1.0')
        title= page_title


        link(rel='stylesheet', href='/static/fonts.css')
        link(rel='stylesheet', href='/static/icons.css')
        link(rel='stylesheet', href='/static/material.cyan-light_blue.min.css')
        link(rel='stylesheet', href='/static/styles.css')
        link(rel="stylesheet" href="/static/pikaday.css")
        style.
            #view-source {
                position: fixed;
                display: block;
                right: 0;
                bottom: 0;
                margin-right: 40px;
                margin-bottom: 40px;
                z-index: 900;
            }

    body
        .demo-layout.mdl-layout.mdl-js-layout.mdl-layout--fixed-drawer.mdl-layout--fixed-header
            header.demo-header.mdl-layout__header.mdl-color--grey-100.mdl-color-text--grey-600
                .mdl-layout__header-row
                    block header
                    .mdl-layout-spacer
                    .mdl-textfield.mdl-js-textfield.mdl-textfield--expandable
                        label.mdl-button.mdl-js-button.mdl-button--icon(for='search')
                            i.material-icons search
                        .mdl-textfield__expandable-holder
                            input#search.mdl-textfield__input(type='text')
                            label.mdl-textfield__label(for='search') Enter your query...
                    div
                        button#datepicker.mdl-button.mdl-button--icon
                            i.material-icons event
                        input#date(style="display:none" name="date")

            .demo-drawer.mdl-layout__drawer.mdl-color--blue-grey-900.mdl-color-text--blue-grey-50
                header.demo-drawer-header
                nav.demo-navigation.mdl-navigation.mdl-color--blue-grey-800
                    a.mdl-navigation__link(href='/admin/day')
                        i
                        |  Today
                    a.mdl-navigation__link(href='/admin/days')
                        i
                        |  Days
            main.mdl-layout__content.mdl-color--grey-100
                .mdl-grid.demo-content
                    block content
        a#view-source.mdl-button.mdl-js-button.mdl-button--raised.mdl-js-ripple-effect.mdl-button--colored.mdl-color-text--white(href='https://kdsapi.org/schedule/docs/', target='_blank') Read the Docs
        script(src='/static/material.min.js')
        script(src='/static/moment.min.js')
        script(src='/static/pikaday.js')
        script(src='/static/admin_scripts.js')

